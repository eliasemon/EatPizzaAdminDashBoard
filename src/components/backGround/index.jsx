import { styled } from "@mui/system";

const Background = styled("div")({
  background: "#434f63",
  zIndex: -1,
  position: "absolute",
  top: 0,
  left: 0,
  right: 0,
});

export default Background;
